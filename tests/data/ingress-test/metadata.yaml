name: ingress-test
display-name: Ingress Test
description: Test for ingress relation
summary: Test for ingress relation

requires:
  ingress:
    interface: ingress
    limit: 1

containers:
  httpbin:
    resource: httpbin-image

resources:
  httpbin-image:
    type: oci-image
    description: OCI image for httpbin (kennethreitz/httpbin)

---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ service }}
spec:
  gateways:
  - '{{ namespace }}/{{ default_gateway }}'
  {% for i in custom_gateways %}
  - '{{ i }}'
  {% endfor %}
  hosts:
  - '*'
  http:
  - match:
    - uri:
        prefix: '{{ prefix }}'
    rewrite:
      uri: '{{ rewrite or prefix }}'
    route:
    - destination:
        host: '{{ service }}.{{ namespace }}.svc.cluster.local'
        port:
          number: {{ port }}
